.Overlay {
  height: 100vh;
  width: 100vw;
  background: white;
  position: fixed;
  overflow: hidden;
  overflow-y: scroll;
  -webkit-overflow-scrolling: touch;
  left: 0;
  top: 0;
  z-index: 999;
  transition: all 200ms ease-out;
  opacity: 0;
  // transform: scale(1.03);
  transform: translateY(10px);
}
.Overlay.Active {
  opacity: 1;
  // transform: scale(1);
  transform: translateY(0px);
}
.OverlayClose {
  position: fixed;
  top: 8px;
  left: 8px;
  height: 32px;
  width: 32px;
  background: url(_img/_icon/dark/close.svg) no-repeat;
  background-size: 24px;
  background-position: center center;
  border-radius: 10000px;
  border: none;
  outline: none;
  text-indent: -999999px;
}
.OverlayContent {
  margin-top: 75px;
}
